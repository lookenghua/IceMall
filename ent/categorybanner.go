// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"ice-mall/ent/categorybanner"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// CategoryBanner is the model entity for the CategoryBanner schema.
type CategoryBanner struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*CategoryBanner) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case categorybanner.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type CategoryBanner", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the CategoryBanner fields.
func (cb *CategoryBanner) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case categorybanner.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			cb.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this CategoryBanner.
// Note that you need to call CategoryBanner.Unwrap() before calling this method if this CategoryBanner
// was returned from a transaction, and the transaction was committed or rolled back.
func (cb *CategoryBanner) Update() *CategoryBannerUpdateOne {
	return (&CategoryBannerClient{config: cb.config}).UpdateOne(cb)
}

// Unwrap unwraps the CategoryBanner entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (cb *CategoryBanner) Unwrap() *CategoryBanner {
	tx, ok := cb.config.driver.(*txDriver)
	if !ok {
		panic("ent: CategoryBanner is not a transactional entity")
	}
	cb.config.driver = tx.drv
	return cb
}

// String implements the fmt.Stringer.
func (cb *CategoryBanner) String() string {
	var builder strings.Builder
	builder.WriteString("CategoryBanner(")
	builder.WriteString(fmt.Sprintf("id=%v", cb.ID))
	builder.WriteByte(')')
	return builder.String()
}

// CategoryBanners is a parsable slice of CategoryBanner.
type CategoryBanners []*CategoryBanner

func (cb CategoryBanners) config(cfg config) {
	for _i := range cb {
		cb[_i].config = cfg
	}
}
