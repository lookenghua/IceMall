// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"ice-mall/ent/datadictionarykey"
	"ice-mall/ent/predicate"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// DataDictionaryKeyUpdate is the builder for updating DataDictionaryKey entities.
type DataDictionaryKeyUpdate struct {
	config
	hooks    []Hook
	mutation *DataDictionaryKeyMutation
}

// Where appends a list predicates to the DataDictionaryKeyUpdate builder.
func (ddku *DataDictionaryKeyUpdate) Where(ps ...predicate.DataDictionaryKey) *DataDictionaryKeyUpdate {
	ddku.mutation.Where(ps...)
	return ddku
}

// Mutation returns the DataDictionaryKeyMutation object of the builder.
func (ddku *DataDictionaryKeyUpdate) Mutation() *DataDictionaryKeyMutation {
	return ddku.mutation
}

// Save executes the query and returns the number of nodes affected by the update operation.
func (ddku *DataDictionaryKeyUpdate) Save(ctx context.Context) (int, error) {
	var (
		err      error
		affected int
	)
	if len(ddku.hooks) == 0 {
		affected, err = ddku.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*DataDictionaryKeyMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			ddku.mutation = mutation
			affected, err = ddku.sqlSave(ctx)
			mutation.done = true
			return affected, err
		})
		for i := len(ddku.hooks) - 1; i >= 0; i-- {
			if ddku.hooks[i] == nil {
				return 0, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = ddku.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ddku.mutation); err != nil {
			return 0, err
		}
	}
	return affected, err
}

// SaveX is like Save, but panics if an error occurs.
func (ddku *DataDictionaryKeyUpdate) SaveX(ctx context.Context) int {
	affected, err := ddku.Save(ctx)
	if err != nil {
		panic(err)
	}
	return affected
}

// Exec executes the query.
func (ddku *DataDictionaryKeyUpdate) Exec(ctx context.Context) error {
	_, err := ddku.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ddku *DataDictionaryKeyUpdate) ExecX(ctx context.Context) {
	if err := ddku.Exec(ctx); err != nil {
		panic(err)
	}
}

func (ddku *DataDictionaryKeyUpdate) sqlSave(ctx context.Context) (n int, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   datadictionarykey.Table,
			Columns: datadictionarykey.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: datadictionarykey.FieldID,
			},
		},
	}
	if ps := ddku.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if n, err = sqlgraph.UpdateNodes(ctx, ddku.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{datadictionarykey.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return 0, err
	}
	return n, nil
}

// DataDictionaryKeyUpdateOne is the builder for updating a single DataDictionaryKey entity.
type DataDictionaryKeyUpdateOne struct {
	config
	fields   []string
	hooks    []Hook
	mutation *DataDictionaryKeyMutation
}

// Mutation returns the DataDictionaryKeyMutation object of the builder.
func (ddkuo *DataDictionaryKeyUpdateOne) Mutation() *DataDictionaryKeyMutation {
	return ddkuo.mutation
}

// Select allows selecting one or more fields (columns) of the returned entity.
// The default is selecting all fields defined in the entity schema.
func (ddkuo *DataDictionaryKeyUpdateOne) Select(field string, fields ...string) *DataDictionaryKeyUpdateOne {
	ddkuo.fields = append([]string{field}, fields...)
	return ddkuo
}

// Save executes the query and returns the updated DataDictionaryKey entity.
func (ddkuo *DataDictionaryKeyUpdateOne) Save(ctx context.Context) (*DataDictionaryKey, error) {
	var (
		err  error
		node *DataDictionaryKey
	)
	if len(ddkuo.hooks) == 0 {
		node, err = ddkuo.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*DataDictionaryKeyMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			ddkuo.mutation = mutation
			node, err = ddkuo.sqlSave(ctx)
			mutation.done = true
			return node, err
		})
		for i := len(ddkuo.hooks) - 1; i >= 0; i-- {
			if ddkuo.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = ddkuo.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, ddkuo.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX is like Save, but panics if an error occurs.
func (ddkuo *DataDictionaryKeyUpdateOne) SaveX(ctx context.Context) *DataDictionaryKey {
	node, err := ddkuo.Save(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// Exec executes the query on the entity.
func (ddkuo *DataDictionaryKeyUpdateOne) Exec(ctx context.Context) error {
	_, err := ddkuo.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (ddkuo *DataDictionaryKeyUpdateOne) ExecX(ctx context.Context) {
	if err := ddkuo.Exec(ctx); err != nil {
		panic(err)
	}
}

func (ddkuo *DataDictionaryKeyUpdateOne) sqlSave(ctx context.Context) (_node *DataDictionaryKey, err error) {
	_spec := &sqlgraph.UpdateSpec{
		Node: &sqlgraph.NodeSpec{
			Table:   datadictionarykey.Table,
			Columns: datadictionarykey.Columns,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: datadictionarykey.FieldID,
			},
		},
	}
	id, ok := ddkuo.mutation.ID()
	if !ok {
		return nil, &ValidationError{Name: "id", err: errors.New(`ent: missing "DataDictionaryKey.id" for update`)}
	}
	_spec.Node.ID.Value = id
	if fields := ddkuo.fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, datadictionarykey.FieldID)
		for _, f := range fields {
			if !datadictionarykey.ValidColumn(f) {
				return nil, &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
			}
			if f != datadictionarykey.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, f)
			}
		}
	}
	if ps := ddkuo.mutation.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	_node = &DataDictionaryKey{config: ddkuo.config}
	_spec.Assign = _node.assignValues
	_spec.ScanValues = _node.scanValues
	if err = sqlgraph.UpdateNode(ctx, ddkuo.driver, _spec); err != nil {
		if _, ok := err.(*sqlgraph.NotFoundError); ok {
			err = &NotFoundError{datadictionarykey.Label}
		} else if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	return _node, nil
}
