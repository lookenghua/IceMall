// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"ice-mall/ent/productcollection"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// ProductCollection is the model entity for the ProductCollection schema.
type ProductCollection struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductCollection) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case productcollection.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ProductCollection", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductCollection fields.
func (pc *ProductCollection) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productcollection.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pc.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this ProductCollection.
// Note that you need to call ProductCollection.Unwrap() before calling this method if this ProductCollection
// was returned from a transaction, and the transaction was committed or rolled back.
func (pc *ProductCollection) Update() *ProductCollectionUpdateOne {
	return (&ProductCollectionClient{config: pc.config}).UpdateOne(pc)
}

// Unwrap unwraps the ProductCollection entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pc *ProductCollection) Unwrap() *ProductCollection {
	tx, ok := pc.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductCollection is not a transactional entity")
	}
	pc.config.driver = tx.drv
	return pc
}

// String implements the fmt.Stringer.
func (pc *ProductCollection) String() string {
	var builder strings.Builder
	builder.WriteString("ProductCollection(")
	builder.WriteString(fmt.Sprintf("id=%v", pc.ID))
	builder.WriteByte(')')
	return builder.String()
}

// ProductCollections is a parsable slice of ProductCollection.
type ProductCollections []*ProductCollection

func (pc ProductCollections) config(cfg config) {
	for _i := range pc {
		pc[_i].config = cfg
	}
}
