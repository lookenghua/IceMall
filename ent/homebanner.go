// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"ice-mall/ent/homebanner"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// HomeBanner is the model entity for the HomeBanner schema.
type HomeBanner struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*HomeBanner) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case homebanner.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type HomeBanner", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the HomeBanner fields.
func (hb *HomeBanner) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case homebanner.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			hb.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this HomeBanner.
// Note that you need to call HomeBanner.Unwrap() before calling this method if this HomeBanner
// was returned from a transaction, and the transaction was committed or rolled back.
func (hb *HomeBanner) Update() *HomeBannerUpdateOne {
	return (&HomeBannerClient{config: hb.config}).UpdateOne(hb)
}

// Unwrap unwraps the HomeBanner entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (hb *HomeBanner) Unwrap() *HomeBanner {
	tx, ok := hb.config.driver.(*txDriver)
	if !ok {
		panic("ent: HomeBanner is not a transactional entity")
	}
	hb.config.driver = tx.drv
	return hb
}

// String implements the fmt.Stringer.
func (hb *HomeBanner) String() string {
	var builder strings.Builder
	builder.WriteString("HomeBanner(")
	builder.WriteString(fmt.Sprintf("id=%v", hb.ID))
	builder.WriteByte(')')
	return builder.String()
}

// HomeBanners is a parsable slice of HomeBanner.
type HomeBanners []*HomeBanner

func (hb HomeBanners) config(cfg config) {
	for _i := range hb {
		hb[_i].config = cfg
	}
}
