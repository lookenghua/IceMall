// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"ice-mall/ent/productevaluation"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProductEvaluationCreate is the builder for creating a ProductEvaluation entity.
type ProductEvaluationCreate struct {
	config
	mutation *ProductEvaluationMutation
	hooks    []Hook
}

// Mutation returns the ProductEvaluationMutation object of the builder.
func (pec *ProductEvaluationCreate) Mutation() *ProductEvaluationMutation {
	return pec.mutation
}

// Save creates the ProductEvaluation in the database.
func (pec *ProductEvaluationCreate) Save(ctx context.Context) (*ProductEvaluation, error) {
	var (
		err  error
		node *ProductEvaluation
	)
	if len(pec.hooks) == 0 {
		if err = pec.check(); err != nil {
			return nil, err
		}
		node, err = pec.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ProductEvaluationMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pec.check(); err != nil {
				return nil, err
			}
			pec.mutation = mutation
			if node, err = pec.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pec.hooks) - 1; i >= 0; i-- {
			if pec.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pec.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pec.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pec *ProductEvaluationCreate) SaveX(ctx context.Context) *ProductEvaluation {
	v, err := pec.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pec *ProductEvaluationCreate) Exec(ctx context.Context) error {
	_, err := pec.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pec *ProductEvaluationCreate) ExecX(ctx context.Context) {
	if err := pec.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pec *ProductEvaluationCreate) check() error {
	return nil
}

func (pec *ProductEvaluationCreate) sqlSave(ctx context.Context) (*ProductEvaluation, error) {
	_node, _spec := pec.createSpec()
	if err := sqlgraph.CreateNode(ctx, pec.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pec *ProductEvaluationCreate) createSpec() (*ProductEvaluation, *sqlgraph.CreateSpec) {
	var (
		_node = &ProductEvaluation{config: pec.config}
		_spec = &sqlgraph.CreateSpec{
			Table: productevaluation.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: productevaluation.FieldID,
			},
		}
	)
	return _node, _spec
}

// ProductEvaluationCreateBulk is the builder for creating many ProductEvaluation entities in bulk.
type ProductEvaluationCreateBulk struct {
	config
	builders []*ProductEvaluationCreate
}

// Save creates the ProductEvaluation entities in the database.
func (pecb *ProductEvaluationCreateBulk) Save(ctx context.Context) ([]*ProductEvaluation, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pecb.builders))
	nodes := make([]*ProductEvaluation, len(pecb.builders))
	mutators := make([]Mutator, len(pecb.builders))
	for i := range pecb.builders {
		func(i int, root context.Context) {
			builder := pecb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProductEvaluationMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pecb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pecb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pecb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pecb *ProductEvaluationCreateBulk) SaveX(ctx context.Context) []*ProductEvaluation {
	v, err := pecb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pecb *ProductEvaluationCreateBulk) Exec(ctx context.Context) error {
	_, err := pecb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pecb *ProductEvaluationCreateBulk) ExecX(ctx context.Context) {
	if err := pecb.Exec(ctx); err != nil {
		panic(err)
	}
}
