// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"ice-mall/ent/datadictionarykey"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// DataDictionaryKey is the model entity for the DataDictionaryKey schema.
type DataDictionaryKey struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*DataDictionaryKey) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case datadictionarykey.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type DataDictionaryKey", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the DataDictionaryKey fields.
func (ddk *DataDictionaryKey) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case datadictionarykey.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ddk.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this DataDictionaryKey.
// Note that you need to call DataDictionaryKey.Unwrap() before calling this method if this DataDictionaryKey
// was returned from a transaction, and the transaction was committed or rolled back.
func (ddk *DataDictionaryKey) Update() *DataDictionaryKeyUpdateOne {
	return (&DataDictionaryKeyClient{config: ddk.config}).UpdateOne(ddk)
}

// Unwrap unwraps the DataDictionaryKey entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ddk *DataDictionaryKey) Unwrap() *DataDictionaryKey {
	tx, ok := ddk.config.driver.(*txDriver)
	if !ok {
		panic("ent: DataDictionaryKey is not a transactional entity")
	}
	ddk.config.driver = tx.drv
	return ddk
}

// String implements the fmt.Stringer.
func (ddk *DataDictionaryKey) String() string {
	var builder strings.Builder
	builder.WriteString("DataDictionaryKey(")
	builder.WriteString(fmt.Sprintf("id=%v", ddk.ID))
	builder.WriteByte(')')
	return builder.String()
}

// DataDictionaryKeys is a parsable slice of DataDictionaryKey.
type DataDictionaryKeys []*DataDictionaryKey

func (ddk DataDictionaryKeys) config(cfg config) {
	for _i := range ddk {
		ddk[_i].config = cfg
	}
}
