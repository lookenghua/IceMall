// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"ice-mall/ent/productbrowsehistory"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProductBrowseHistoryCreate is the builder for creating a ProductBrowseHistory entity.
type ProductBrowseHistoryCreate struct {
	config
	mutation *ProductBrowseHistoryMutation
	hooks    []Hook
}

// Mutation returns the ProductBrowseHistoryMutation object of the builder.
func (pbhc *ProductBrowseHistoryCreate) Mutation() *ProductBrowseHistoryMutation {
	return pbhc.mutation
}

// Save creates the ProductBrowseHistory in the database.
func (pbhc *ProductBrowseHistoryCreate) Save(ctx context.Context) (*ProductBrowseHistory, error) {
	var (
		err  error
		node *ProductBrowseHistory
	)
	if len(pbhc.hooks) == 0 {
		if err = pbhc.check(); err != nil {
			return nil, err
		}
		node, err = pbhc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ProductBrowseHistoryMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pbhc.check(); err != nil {
				return nil, err
			}
			pbhc.mutation = mutation
			if node, err = pbhc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pbhc.hooks) - 1; i >= 0; i-- {
			if pbhc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pbhc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pbhc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pbhc *ProductBrowseHistoryCreate) SaveX(ctx context.Context) *ProductBrowseHistory {
	v, err := pbhc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pbhc *ProductBrowseHistoryCreate) Exec(ctx context.Context) error {
	_, err := pbhc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pbhc *ProductBrowseHistoryCreate) ExecX(ctx context.Context) {
	if err := pbhc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pbhc *ProductBrowseHistoryCreate) check() error {
	return nil
}

func (pbhc *ProductBrowseHistoryCreate) sqlSave(ctx context.Context) (*ProductBrowseHistory, error) {
	_node, _spec := pbhc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pbhc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pbhc *ProductBrowseHistoryCreate) createSpec() (*ProductBrowseHistory, *sqlgraph.CreateSpec) {
	var (
		_node = &ProductBrowseHistory{config: pbhc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: productbrowsehistory.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: productbrowsehistory.FieldID,
			},
		}
	)
	return _node, _spec
}

// ProductBrowseHistoryCreateBulk is the builder for creating many ProductBrowseHistory entities in bulk.
type ProductBrowseHistoryCreateBulk struct {
	config
	builders []*ProductBrowseHistoryCreate
}

// Save creates the ProductBrowseHistory entities in the database.
func (pbhcb *ProductBrowseHistoryCreateBulk) Save(ctx context.Context) ([]*ProductBrowseHistory, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pbhcb.builders))
	nodes := make([]*ProductBrowseHistory, len(pbhcb.builders))
	mutators := make([]Mutator, len(pbhcb.builders))
	for i := range pbhcb.builders {
		func(i int, root context.Context) {
			builder := pbhcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProductBrowseHistoryMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pbhcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pbhcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pbhcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pbhcb *ProductBrowseHistoryCreateBulk) SaveX(ctx context.Context) []*ProductBrowseHistory {
	v, err := pbhcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pbhcb *ProductBrowseHistoryCreateBulk) Exec(ctx context.Context) error {
	_, err := pbhcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pbhcb *ProductBrowseHistoryCreateBulk) ExecX(ctx context.Context) {
	if err := pbhcb.Exec(ctx); err != nil {
		panic(err)
	}
}
