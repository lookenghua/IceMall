// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"ice-mall/ent/productfocus"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// ProductFocusCreate is the builder for creating a ProductFocus entity.
type ProductFocusCreate struct {
	config
	mutation *ProductFocusMutation
	hooks    []Hook
}

// Mutation returns the ProductFocusMutation object of the builder.
func (pfc *ProductFocusCreate) Mutation() *ProductFocusMutation {
	return pfc.mutation
}

// Save creates the ProductFocus in the database.
func (pfc *ProductFocusCreate) Save(ctx context.Context) (*ProductFocus, error) {
	var (
		err  error
		node *ProductFocus
	)
	if len(pfc.hooks) == 0 {
		if err = pfc.check(); err != nil {
			return nil, err
		}
		node, err = pfc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*ProductFocusMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pfc.check(); err != nil {
				return nil, err
			}
			pfc.mutation = mutation
			if node, err = pfc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pfc.hooks) - 1; i >= 0; i-- {
			if pfc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pfc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pfc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pfc *ProductFocusCreate) SaveX(ctx context.Context) *ProductFocus {
	v, err := pfc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pfc *ProductFocusCreate) Exec(ctx context.Context) error {
	_, err := pfc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pfc *ProductFocusCreate) ExecX(ctx context.Context) {
	if err := pfc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pfc *ProductFocusCreate) check() error {
	return nil
}

func (pfc *ProductFocusCreate) sqlSave(ctx context.Context) (*ProductFocus, error) {
	_node, _spec := pfc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pfc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pfc *ProductFocusCreate) createSpec() (*ProductFocus, *sqlgraph.CreateSpec) {
	var (
		_node = &ProductFocus{config: pfc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: productfocus.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: productfocus.FieldID,
			},
		}
	)
	return _node, _spec
}

// ProductFocusCreateBulk is the builder for creating many ProductFocus entities in bulk.
type ProductFocusCreateBulk struct {
	config
	builders []*ProductFocusCreate
}

// Save creates the ProductFocus entities in the database.
func (pfcb *ProductFocusCreateBulk) Save(ctx context.Context) ([]*ProductFocus, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pfcb.builders))
	nodes := make([]*ProductFocus, len(pfcb.builders))
	mutators := make([]Mutator, len(pfcb.builders))
	for i := range pfcb.builders {
		func(i int, root context.Context) {
			builder := pfcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*ProductFocusMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pfcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pfcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pfcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pfcb *ProductFocusCreateBulk) SaveX(ctx context.Context) []*ProductFocus {
	v, err := pfcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pfcb *ProductFocusCreateBulk) Exec(ctx context.Context) error {
	_, err := pfcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pfcb *ProductFocusCreateBulk) ExecX(ctx context.Context) {
	if err := pfcb.Exec(ctx); err != nil {
		panic(err)
	}
}
