// Code generated by entc, DO NOT EDIT.

package ent

import (
	"fmt"
	"ice-mall/ent/productfocus"
	"strings"

	"entgo.io/ent/dialect/sql"
)

// ProductFocus is the model entity for the ProductFocus schema.
type ProductFocus struct {
	config
	// ID of the ent.
	ID int `json:"id,omitempty"`
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductFocus) scanValues(columns []string) ([]interface{}, error) {
	values := make([]interface{}, len(columns))
	for i := range columns {
		switch columns[i] {
		case productfocus.FieldID:
			values[i] = new(sql.NullInt64)
		default:
			return nil, fmt.Errorf("unexpected column %q for type ProductFocus", columns[i])
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductFocus fields.
func (pf *ProductFocus) assignValues(columns []string, values []interface{}) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case productfocus.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			pf.ID = int(value.Int64)
		}
	}
	return nil
}

// Update returns a builder for updating this ProductFocus.
// Note that you need to call ProductFocus.Unwrap() before calling this method if this ProductFocus
// was returned from a transaction, and the transaction was committed or rolled back.
func (pf *ProductFocus) Update() *ProductFocusUpdateOne {
	return (&ProductFocusClient{config: pf.config}).UpdateOne(pf)
}

// Unwrap unwraps the ProductFocus entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (pf *ProductFocus) Unwrap() *ProductFocus {
	tx, ok := pf.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductFocus is not a transactional entity")
	}
	pf.config.driver = tx.drv
	return pf
}

// String implements the fmt.Stringer.
func (pf *ProductFocus) String() string {
	var builder strings.Builder
	builder.WriteString("ProductFocus(")
	builder.WriteString(fmt.Sprintf("id=%v", pf.ID))
	builder.WriteByte(')')
	return builder.String()
}

// ProductFocusSlice is a parsable slice of ProductFocus.
type ProductFocusSlice []*ProductFocus

func (pf ProductFocusSlice) config(cfg config) {
	for _i := range pf {
		pf[_i].config = cfg
	}
}
